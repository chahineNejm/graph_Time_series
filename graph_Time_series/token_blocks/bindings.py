"""Removed: binder/glue tokens are obsolete.

Models now consume the Signal-board auto-bundle (``state.feature_bundle()``)
directly, so no token is needed to assemble ``model_input``. See
SIGNAL_ARCHITECTURE.md. This module is intentionally empty.
"""
